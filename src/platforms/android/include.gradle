/* Include.gradle configuration: http://docs.nativescript.org/plugins/plugins#includegradle-specification */

android {
	
}

dependencies {
    // Describe plugin native Android dependencies like
	// implementation "groupName:pluginName:ver" 
    // EXAMPLE: implementation "com.facebook.fresco:fresco:0.9.0+"

    /**
     * Variant 1:
     * When using ausweisapp-sdk-wrapper.aar in src/platforms/android folder 
     * then setting this deps is required
     */
    // Ausweisapp SDK
    // implementation 'com.governikus:ausweisapp:1.16.+'
    // HTTP Client
    // implementation 'com.squareup.okhttp:okhttp:2.+'

    /*
     * Variant 2:
     * Using as maven dependency (from Bintray) so it is not required to 
     * set any dependency and having a local copy of 
     * ausweisapp-sdk-wrapper.aar in src/platforms/android folder 
     */
    implementation 'de.superfusion:ausweisapp-sdk-wrapper:1.0.0'
}

repositories {
    google()
    jcenter()
	maven {
		url  "https://dl.bintray.com/superfusion/mvn-repo"
	}
}